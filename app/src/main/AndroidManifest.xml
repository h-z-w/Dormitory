<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dormitory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Look3Activity"></activity>
        <activity android:name=".ui.RoomActivity" />
        <activity android:name=".ui.Modify2Activity" />
        <activity android:name=".ui.ModifyActivity" />
        <activity android:name=".ui.Look2Activity" />
        <activity android:name=".ui.GaiActivity" />
        <activity android:name=".ui.LookActivity" />
        <activity android:name=".ui.Main2Activity" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.StudentAddActivity" />
        <activity android:name=".ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>